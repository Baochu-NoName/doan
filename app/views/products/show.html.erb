<p>
   <%= image_tag @product.image, size:'500x400' %>
</p>
<p>
  <strong>Product name:</strong>
  <%= @product.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @product.description %>
</p>

<p>
  <strong>Rating:</strong>
  <% if @product.rating.blank? %>
  <% else %>
  <%= render_stars(@product.rating) %>
  <%= @product.rating %>
  <%end%>
</p>

<%= form_with model: @order_item, remote: true do |f| %>
<%= f.hidden_field :product_id, value: @product.id %>
<%= f.number_field :quantity, value: 1, min: 1 %>
<%= f.button :submit do%>
<i class="fa fa-shopping-cart">Add to cart</i>
<%end%>
  

<!-- <#%= link_to "Add to cart", order_items_path(@product.id), method: :post%> -->
<!-- <#%= link_to "add to cart", add_to_cart_path(@product.id), method: :post%> -->
<%end%>


<%= link_to 'Back', products_path %>